@*@model IEnumerable<ESCOM_FLEET_SYSTEM.Models.Department>*@
@model PaginatedList<ESCOM_FLEET_SYSTEM.Models.Department>

@{
    ViewData["Title"] = "Index";
}

@*<h1>Index</h1>*@

@*<p>
        <a asp-action="Create">Create New</a>
    </p>*@
@*<h3 class="badge-secondar "> STATIONS</h3>*@

<p>
    <a asp-action="Create" class="bg-transparent text-capitalize text-center text-success btn-outline-dark btn-sm my-0"> <i class="fas fa-plus-circle"></i> ADD DEPARTMENT</a>
</p>

<form asp-action="Index" method="get">
    <div class="form-actions no-color">
        <p>
            Department Name ||  <input type="text" name="SearchString" value="@ViewData["CurrentFilter"]" />
            <input type="submit" value="Search" class=" bg-transparent btn btn-sm btn-outline-warning" /> |
            @*<a asp-action="Index" class="bg-warning btn btn-outline-primary sm">Back to Full List</a>*@
        </p>
    </div>
</form>
@*<table class="table">
    <thead>*@
<table class="table table-sm">
    <thead class="table-light">
        <tr>
            <th>
                @*@Html.DisplayNameFor(model => model.DepartmentName)*@
                DepartmentName
            </th>
            <th>
                @*@Html.DisplayNameFor(model => model.Comment)*@
                Comment
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.DepartmentName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Comment)
                </td>
                <td>
                    @*<a asp-action="Edit" asp-route-id="@item.DepartmentId">Edit</a> |
        <a asp-action="Details" asp-route-id="@item.DepartmentId">Details</a> |
        <a asp-action="Delete" asp-route-id="@item.DepartmentId">Delete</a>*@
                    <a class="btn btn-outline-success btn-rounded btn-sm my-0" asp-action="Edit" asp-route-id="@item.DepartmentId">Edit</a> |
                    <a class="btn btn-outline-info btn-rounded btn-sm my-0" asp-action="Details" asp-route-id="@item.DepartmentId">Details</a> |
                    <a class="btn btn-outline-danger btn-rounded btn-sm my-0" asp-action="Delete" asp-route-id="@item.DepartmentId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
